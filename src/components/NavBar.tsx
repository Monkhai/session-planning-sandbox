import React from "react";

const NavBar = () => {
  return (
    <nav className="bg-darkBackground sticky flex w-full justify-center">
      <h1 className="p-5 text-white">Session Sandbox</h1>
    </nav>
  );
};

export default NavBar;
