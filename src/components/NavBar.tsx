import React from "react";

const NavBar = () => {
  return (
    <nav className="sticky flex w-full justify-center bg-darkBackground print:hidden">
      <h1 className="p-5 text-white">Session Sandbox</h1>
    </nav>
  );
};

export default NavBar;
